"""Parser test fixtures.

Concrete sample files (PDF, DOCX, MD, TXT, VTT, SRT, CSV, XLSX, MBOX, JSON) used
by both unit parser tests and integration agent tests. Regenerate via
``make_fixtures.py``.
"""
